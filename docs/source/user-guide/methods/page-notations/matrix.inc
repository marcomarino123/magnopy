A matrix is denoted with a bold symbol. An example is the exchange tensor between two arbitrary sites
:math:`i` and :math:`j`, that is denoted :math:`\boldsymbol{J}_{ij}`.
Its matrix elements :math:`(\,\alpha,\,\beta\,)` are denoted as :math:`J_{ij}^{\alpha \beta}`.
